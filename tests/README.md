# Tests

Version 0.3.0 adds no runtime software capability.

Release checks cover JSON parseability, sample-selection reproducibility, source and sample checksums, imported-reference preservation, documentation links, and Rust formatting/build checks where the required toolchain is available.
