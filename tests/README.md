# Tests

Automated tests will be added with software capabilities.

Planned suites include schema-resolution tests, official valid/invalid example tests, exact-model round trips, normalization traceability, geometry and time edge cases, CLI output/exit-code tests, and provenance integrity checks.

v0.2.1 intentionally contains zero Rust tests because it adds no validator or parser behaviour.
